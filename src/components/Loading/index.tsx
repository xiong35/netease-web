
import "./index.scss";

// type LoadingProps = {
// };

function Loading(/* props: LoadingProps */) {
  // const {} = props;

  return (
    <div className="loading">
      Loading
    </div>
  );
}

export default Loading;
